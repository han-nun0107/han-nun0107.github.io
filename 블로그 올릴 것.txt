호이스팅이 정확하게 어떻게 일어나는지
ex) 평가 실행를 넣어서

어떤 방식으로 진행 되는지 - 평가 과정에서 각 변수, 함수를 메모리에 저장 시킴 -> 실행 단계에서 값을 넣어줌
자스는 인터프리터 언어라서 위에서 아래로 한 줄씩 차례로 실행이 됨.
근데 만약 function 키워드로 맨 아래에 선언된 foo라는 함수가 있고 상단에 foo()가 있다면 foo()가 뭐야?하고 레퍼런스 에러가 발생함
하지만 창시자는 그것이 싫어 평가 단계에서 한번 훑고 실행 단계에서 값을 넣어줌으로써 이 문제를 해결함

파이썬의 경우 위에서 아래로 단 한 번만 판단하기 때문에 바로 에러가 나와 터지게 됌